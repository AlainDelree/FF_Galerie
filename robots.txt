# frederiqueferette.be — robots.txt
# Le contenu est ouvert au crawl (y compris crawlers IA : GPTBot, ClaudeBot,
# OAI-SearchBot, PerplexityBot…), car ils ne rendent pas le JS : le contenu
# statique baké (meta, JSON-LD, alt) est leur seule source.

User-agent: *
Allow: /
Disallow: /admin.html
# Artistes invités encore en brouillon (draft + noindex). À retirer quand un
# artiste passe en ligne.
Disallow: /artistes/

Sitemap: https://frederiqueferette.be/sitemap.xml
